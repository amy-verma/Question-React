// React.memo remembers the last result of a component and skips re-rendering it if
//  the props haven't changed.

// What it does:
// Wraps a functional component to memoize its output. It will only re-render if props change.